<h3>Board</h3>

<div class="board-grid">
</div>

@code {

}

<style>
    :root {
        --white-tile: white;
        --black-tile: black;
    }

    .board-grid {
        width: 400px;
        height: 400px;
        border: 1px solid black;
        border-radius: 5px;
        display: grid;
        grid-template-columns: repeat(8, 50px);
        grid-template-rows: repeat(8, 50px);
        background: repeating-conic-gradient(var(--black-tile) 0% 25%, var(--white-tile) 0% 50%) 0 / 100px 100px;
    }
</style>
